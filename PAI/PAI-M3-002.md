Zadanie PAI-M3-002. Statystyka ocen**

Dana jest tablica `$oceny = [4, 5, 3, 6, 2, 5, 4, 3, 5];`

1. Wypisz wszystkie oceny po przecinku jednym poleceniem (bez pętli).
2. Wypisz liczbę ocen, sumę, średnią zaokrągloną do 2 miejsc, ocenę najwyższą i najniższą.
3. Pętlą policz, ile jest ocen co najmniej dobrych (≥ 4).
4. **Dla chętnych:** Napisz funkcję `statystyki(array $t): array`, która zwraca tablicę asocjacyjną z kluczami `ile`, `suma`, `srednia`, `min`, `max`. Wyświetl jej wynik przez `print_r` w `<pre>`.
